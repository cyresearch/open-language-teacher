# 安全设计

本项目的威胁模型和护栏。设计上宁可少一个功能，不多一个攻击面。
（部分教训来自 OpenClaw 技能市场安全危机的公开研究：可执行的社区扩展 + 提示注入是结构性风险。）

## 五条护栏

1. **配置是数据，不是代码**
   `config/*.md` 只描述人设、画像与课程，引擎只把它们当文本读。本项目不做可执行的「技能市场」，不运行来自配置的任何指令。

2. **老师大脑最小权限**
   老师的 LLM 调用只开放 WebSearch / WebFetch / Read。没有 Bash，没有 Write，没有除「回消息给学生」之外的任何出口。即使网页内容里埋了恶意指令，它也没有手脚可用。

3. **密钥纪律**
   所有 token/key 只住仓库根的 `.env`（已 gitignore）或系统钥匙串。不进仓库、不进 prompt、不进日志。提交前请自查：`git grep -iE "token|key|secret"` 不应出现真实凭据。

4. **聊天通道默认拒绝**
   Discord 值班员只应答 `.env` 里指定的 OWNER_ID，其余一切来信一律无视。不要把 bot 加进公开服务器后开放应答范围。

5. **合规的大脑接入**
   只用第一方途径调用 LLM（自己的 Claude Code、自己的 API key、或本地模型）。绝不做任何绕过服务条款的「订阅搭车」。

## 数据流向（谁会看到什么）

- 你的语音与文字：本机听写（Whisper 本地运行，不上传）
- LLM 请求：发往你自己选择的大脑（Claude / 本地模型）
- 语音合成：默认全本地；只有 edge 兜底线会把**要朗读的文本**发给微软在线服务
- 课堂记录：只写本机 `lessons/`

## 报告漏洞

发 issue 或联系维护者（首个公开版本时补充具体方式）。
